# Intent

What we are doing and why.

Put here: briefs, campaign goals, task definitions, research questions, requirements.

Not here: source files or working notes (→ `context/sources/`), outputs (→ `output/`).

Subfolders when needed:

- `campaigns/` — one subfolder per campaign with its own brief
- `tasks/` — broken-down work items when a brief produces many steps
