# Workbench Input

Drop files for the current task here when no more specific project location is needed.

If a task does not specify where to read files from, assistants should inspect this folder first.

Contents are ignored by Git by default.

