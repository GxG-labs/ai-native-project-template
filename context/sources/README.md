# Sources

What we know from the outside world.

Put here: articles, external references, working notes, findings, synthesis.

Files can start as raw sources and gain annotations over time — no need to split them into separate "references" and "research" folders.

Not here: brand identity (→ `brand/`), final outputs (→ `output/`).
